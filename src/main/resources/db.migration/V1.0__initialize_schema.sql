create table sequencia (
                         id     VARCHAR(60)  NOT NULL,
                         labSeq VARCHAR(9000)  NOT NULL,
                         numero numeric(18) NOT NULL,
                         CONSTRAINT sequencia_pkey PRIMARY KEY (id)
                         );